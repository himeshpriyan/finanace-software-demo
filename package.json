{
  "name": "shop-erp",
  "version": "1.0.0",
  "description": "Shop ERP for Sales, Purchases, Banking, and Reports (Standalone Mock Data)",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  }
}
